# Examples

Runnable demo scripts. Each should activate this package's environment and
exercise one feature, ideally mirroring a note in `notes/`.

Run one with, from the repo root:

```
julia --project=. examples/<script>.jl
```
